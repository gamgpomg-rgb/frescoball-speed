# ピンク球検出更新の検証（2026-09-13）

対象: IMG_0001.MOV の180–188秒と240–248秒。各240フレーム、640×360 RGBA、30fps。既存の同区間骨格を再利用し、旧 balls と新 createDetector を同じ画素列で比較した。旧版は713ede9。

|区間|旧候補数|新候補数|旧軌跡表示フレーム|新軌跡表示フレーム|
|---|---:|---:|---:|---:|
|180–188秒|505|418|220|224|
|240–248秒|898|761|216|212|

双方とも追跡12本。表示フレーム数は正解率ではない。変更箇所を原映像に描画して確認し、初期試作の肌・脚への誤追尾を受けて、色の緩和を選手の枠外に限定し、関節・手足の線付近を除外した。全480フレームの正解位置を人手で付けた検証ではない。背景の人・車を完全に識別する意味モデルではない。

Node上の画素抽出部分: 旧約386/364ms、新約793/573ms（各8秒区間）。追加負荷は1フレームあたり約1.7/0.9ms。スマホ実機や動画全体の処理時間ではない。

再現用のローカル比較: /tmp/compare-detector.cjs, /tmp/extract-ball-frames.py, /tmp/render-detector.cjs。比較データ: /tmp/frescoball-video-check/detector-comparison.json と detector-{180,240}.json。一時ファイルのため恒久的なテスト入力ではない。

永続テストは test_ball_detector.js。静止ピンク、大きな移動物、淡いピンクの局所回復、ブレ、予測のみでは点を作らないこと、低速背景を検証。既存のモーション再開テストも新検出器を含めて実行。球速計算モジュールは変更していない。

途中保存キーはversion 2。旧版の途中保存を新アルゴリズムの結果として再利用しない。旧完成記録は閲覧できるが、新しい追尾を適用するには再解析が必要。新バージョン同士の中断・再開は継続可能。再開境界では背景と予測を再初期化するため数フレームの候補が連続実行と異なる可能性がある。

# 空を背景にしたロブの検出（2026-09-13）

対象: IMG_9996.MOV（スマホ出力 frescoball-original-skeleton-1789282150633.mp4 の元動画、14.48〜75.08s）。スマホ出力で軌跡が消える区間（出力13.2〜15.0s＝元27.7〜29.5s）は、Node 上の同じ検出器でも同じ位置で途切れた。端末差ではない。原寸でボールは約8px・RGB(105,65,65) の暗い赤で、640px では明るい空 (199,214,226) と混ざり赤・ピンク・弱い色のどの規則にも当てはまらなかった。

追加した規則 skyDark: 選手枠の上6割より上で、背景（EMA）が明るく青みがかった無彩色（空）で、上下左右3px先の背景も空、画素が背景より十分暗く（合計差90以上）かつ背景より赤い（r−g の差18以上）点。選手枠の内側では使わない。ガラス窓に映った空の前を歩く人の頭は「下に体がある」ため外れる。

|区間（骨格なし・枠は頭〜膝）|変更前|変更後|
|---|---|---|
|IMG_9996 26.5〜32.5s（ロブ）|追跡8本・観測90|追跡9本・観測146（27.8〜29.4s の空白が埋まる）|
|IMG_9996 14.5/44.5/64.5s 各10s・返球47|飛行中の観測 453/508（89%）・観測率60%以上の返球 39|492/508（97%）・45|
|IMG_9997 98.5〜102.5s|追跡6本・観測110|追跡6本・観測112|
|IMG_0001 180〜188s|追跡12本・観測219|同じ|
|IMG_0001 240〜248s（通行人）|候補1033・追跡12本・観測215|候補1071・追跡12本・観測215|

検出コストは Node で +0.2〜0.3ms/フレーム。正解ラベルとの比較ではなく、音声の返球区間内に観測点があるかの計数。再現: scratchpad の rgba 抽出＋Node 比較（一時ファイル）。永続テストは test_ball_detector.js の「Sky-contrast lob candidates」。

# 予測位置の周りの原寸局所探索と仮説の持ち越し（2026-09-13）

- 持ち越し: 見失った仮説（速度あり）を0.1秒だけ残す。640px の弱い色の回復と予測位置の局所探索の両方に効く。
- 局所探索 refineLocal: 次フレームの予測位置（無ければ打音直後0.2秒の手首）の周りに原寸の小窓（128/160px）を切り出し、前フレームの同じ窓との差分＋赤/ピンクの色規則で「球らしい塊」（面積10〜300px・縦横比3.5以下・充填率0.3以上・窓中心から40%以内）を1つだけ返す。看板など大きな赤い領域の一部は除外。骨格の関節（手首12px・他30px）と関節を結ぶ線分（28px）の周りは除外。
- 途中保存のジョブ版を 3→4。

Node ハーネス（JPEG q2 のフレーム・cv2 抽出とは画素が少し違う。骨格は IMG_0001 のみ既存推定を使用。9996/9997 は骨格なし）:

|区間|本番（変更前）|持ち越しのみ|持ち越し＋局所探索|
|---|---|---|---|
|IMG_9996 26.5〜32.5（返球10）|飛行中73%・打点±0.1s 52%|83%・60%|83%・60%（局所追加4は骨格なしのためパンツの模様）|
|IMG_9997 98.5〜102.5（返球6）|97%・86%|97%・86%|100%・97%（局所追加12）|
|IMG_0001 180〜188（返球12）|86%・79%|88%・82%|94%・82%（局所追加9）|

Chrome のアプリ本体（簡易モード・実骨格・頭〜膝の枠）で IMG_9997 98.5〜102.5s: 6返球すべて追跡、観測点 111→114、偽軌跡なし。処理時間は Node で +0.5〜2ms/フレーム。局所探索の追加点は目視で球上にあることを確認（9997/0001）。正解ラベルとの比較ではない。

# 放物線予測（2026-09-13 夜）

- 仮説・追跡の予測を「直線」から「等速＋画像上の重力」の放物線にした。重力は選手枠の中心間隔を7mとして換算（9.8/7×間隔 px/s²）。枠が無いときは0（従来どおりの直線）。
- 加速度は観測から推定しない。10点の2次当てはめは手持ちの揺れやブレで暴れ、速度の推定まで狂って IMG_0001 で飛行中94→90%・打点±0.1s 82→69%に落ちた。速度も直前2点のまま（4点・10点の平滑化は打点付近の観測が1〜9点減った）。
- 見失った仮説の持ち越しを0.1→0.2秒に延長。1点だけの仮説も次フレームまで残す（原寸の局所探索だけで見えている球が、640pxの検出に頼らず2点目とつながる）。
- 表示の橋渡しと欠測補間も放物線。山なりの返球の長い欠測（最長0.9秒）を弦で切らずに弧でつなぐ。速度の整合判定も重力ぶんを補正。
- sw v41。

Node ハーネス（同じ3区間・骨格は IMG_0001 のみ）: 3区間とも本番（6ef07fe）と同じ観測数（IMG_9996 の打点±0.1s だけ 36→37/60）。処理時間は同程度（学習を並走させたため計測は不安定）。

|区間|6ef07fe|放物線|
|---|---|---|
|IMG_9996 26.5〜32.5（返球10）|飛行中83%・打点±0.1s 60%|83%・62%|
|IMG_9997 98.5〜102.5（返球6）|100%・97%|100%・97%|
|IMG_0001 180〜188（返球12）|94%・82%|94%・82%|

永続テスト: test_ball_detector.js「Parabolic motion model and carried predictions」、test_ball_tracker.js「Parabolic display bridge and gap interpolation」。ブラウザ本体での再解析は未実施（変更は運動モデルと橋渡しの形のみ）。

# 学習モデルの組み込み（2026-09-15）

- 高精度（RF-DETR nano、113MB、PC の Chrome・WebGPU）と軽量（YOLOX-Nano、3.5MB、スマホなど）を、規則ベースが予測窓で何も見つけないときだけ当てる。速度・打数は音声基準のまま。
- 学習データ: 400 枚のラベルから切り抜き 1,468 枚（学習）と 373 枚（検証、IMG_9997 のみ）。
- 検証（切り抜き 373 枚・球 148 個）: 高精度 148/148・誤検出 5、軽量 148/148・誤検出 1（しきい値 0.5／0.4）。
- 規則ベースが見逃した 26 フレーム（3 区間）の回収: 高精度 26/26、軽量 23/26（残り 3 枚は位置は正しく確信度 0.11〜0.37）。
- アプリ本体（IMG_9996 26.5〜32.5s、既定の枠、実骨格）: 飛行中の観測 107/124（86%）→ 高精度 123/124、軽量 123/124。IMG_0001 180〜188s: 162/164 → 163/164。IMG_9997: 78/78 のまま。
- 1 窓あたり（Chrome、M1 Mac）: 高精度 112ms（WebGPU）、軽量 13ms（WebGPU）／73ms（wasm 1 スレッド）。
- 別コート・逆光・別の球色は未検証。永続テスト: tests/test_teacher_model.js、tests/test_app_regressions.js（manifest の整合）。
