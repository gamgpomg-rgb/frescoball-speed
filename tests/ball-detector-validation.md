# ピンク球検出更新の検証（2026-09-13）

対象: IMG_0001.MOV の180–188秒と240–248秒。各240フレーム、640×360 RGBA、30fps。既存の同区間骨格を再利用し、旧 balls と新 createDetector を同じ画素列で比較した。旧版は713ede9。

|区間|旧候補数|新候補数|旧軌跡表示フレーム|新軌跡表示フレーム|
|---|---:|---:|---:|---:|
|180–188秒|505|418|220|224|
|240–248秒|898|761|216|212|

双方とも追跡12本。表示フレーム数は正解率ではない。変更箇所を原映像に描画して確認し、初期試作の肌・脚への誤追尾を受けて、色の緩和を選手の枠外に限定し、関節・手足の線付近を除外した。全480フレームの正解位置を人手で付けた検証ではない。背景の人・車を完全に識別する意味モデルではない。

Node上の画素抽出部分: 旧約386/364ms、新約793/573ms（各8秒区間）。追加負荷は1フレームあたり約1.7/0.9ms。スマホ実機や動画全体の処理時間ではない。

再現用のローカル比較: /tmp/compare-detector.cjs, /tmp/extract-ball-frames.py, /tmp/render-detector.cjs。比較データ: /tmp/frescoball-video-check/detector-comparison.json と detector-{180,240}.json。一時ファイルのため恒久的なテスト入力ではない。

永続テストは test_ball_detector.js。静止ピンク、大きな移動物、淡いピンクの局所回復、ブレ、予測のみでは点を作らないこと、低速背景を検証。既存のモーション再開テストも新検出器を含めて実行。球速計算モジュールは変更していない。

途中保存キーはversion 2。旧版の途中保存を新アルゴリズムの結果として再利用しない。旧完成記録は閲覧できるが、新しい追尾を適用するには再解析が必要。新バージョン同士の中断・再開は継続可能。再開境界では背景と予測を再初期化するため数フレームの候補が連続実行と異なる可能性がある。
